# Voice-to-Text Prototype

Simple prototype using OpenAI Whisper for local transcription.

## Setup

```bash
pip install -r requirements.txt
# Make sure ffmpeg is installed: brew install ffmpeg
```

## Usage

```bash
python transcribe.py
```

## Output

Results saved to: `prototype-test/transcription_result.json`
